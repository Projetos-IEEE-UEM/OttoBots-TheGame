// Este objeto aqui é uma animaçãzinha que acontece quando o Otto pega uma moeda. A linha abaixo basicamente diz que. quando a animação atinge o último quadro,
// a instância deste objeto é destruída.
if image_index > image_number - 1 instance_destroy();