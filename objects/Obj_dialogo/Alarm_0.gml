/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
if inicializar == true{
if caractere < string_length(texto_grid[# Infos.Texto, pagina]){

//var _snd = choose(snd_type_1);
//audio_play_sound(_snd, 1, 0);
caractere++;
alarm[0] = 1;
	}
}